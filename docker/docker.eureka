# Base, ubuntu 이미지
FROM ubuntu:18.04

# jdk 설치
RUN apt-get update && \
    apt-get install -y openjdk-17-jdk && \
    apt-get clean;

WORKDIR /eureka
CMD ["java", "-jar", "/build/libs/eureka-0.0.1-SNAPSHOT.jar"]